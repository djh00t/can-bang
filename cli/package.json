{
  "name": "@can-bang/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mde": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @can-bang/core build && pnpm build && node --test test/index.test.mjs"
  },
  "dependencies": {
    "@can-bang/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
